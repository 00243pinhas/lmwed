export type ActiveRentalSummary = {
  id: string;
  client_name: string;
  due_date: string;
  dress_name: string;
};

export type ActivityItem = {
  id: string;
  timestamp: string;
  description: string;
};
